#include <stdio.h> // import function

int main() // def main() in Python (specifies that an int is returned)
{
    int a = 42;
    char cl = '@';
    char *s = "ahgkjhg";
    // printf() is similar to python print() but DOUBLE QUOTES "" are needed.
    printf("Hello, World!"); // semicolons are mandatory after every line
    return 0; // Not needed but can return 0. Main function returns a value that is returned but not necessary
}

